# requirements for docker + gunicorn + postgresql db + celery

celery==5.2.7
redis==4.3.4 # celery broker
psycopg2==2.9.4 # required for postgresql db backend
gunicorn==20.1.0 # wsgi application
gevent==21.12.0 # worker for gunicorn

